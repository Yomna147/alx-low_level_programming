#include "main.h"
#include <stdio.h>

/**
 * main - checks for uppercase character.
 *
 * Return:1 if c uppercase and 0 otherwise
 */
int _isupper(int c);
{
	char c;

    c = 'A';
    return (1);

    c = 'a';
    return (0);
}
