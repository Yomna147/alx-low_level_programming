#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 *
 * Return: 1 if c is uppercase and 0 othereise
 *
 */

int _isupper(int c);
{
	char c;

if (c >= 'A' && c <= 'Z')
	return (1);
else
	return (0);
}
